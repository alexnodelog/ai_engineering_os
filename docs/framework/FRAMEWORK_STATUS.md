# FRAMEWORK_STATUS.md
 
Status: Active
Framework Version: v10.0 (Tier 1 + Tier 2 — Complete) / v10.1 (Approved pull-forward item complete — `DEC-011` scope fully delivered; remaining v10.1 scope deferred, pending its own Gate 1 approval)
Last Updated: 2026-08-03
 
---
 
# Purpose
 
This document is the single source of truth for the current implementation status of the AI Engineering Operating System Framework.
 
It is intended for both humans and AI assistants.
 
Every new AI session should read this document immediately after FRAMEWORK_README.md.
 
---
 
# Current Phase
 
Phase:
Framework Implementation
 
Current Step:
`project-mobile_v01.md` (Layer 4, Mobile archetype) has been generated and is `Active`. This completes, in full, the scope the Owner approved via Gate 1 decision `DEC-011` — the single v10.1 item pulled forward ahead of the remainder of that scope. All four planned Layer 4 archetype documents (Desktop, Full-Stack, Monolithic, Mobile) are now `Active`. The framework's current work is a housekeeping pass: four documents whose own text pre-declared they would be updated once this transition occurred — `PROJECT_BOOTSTRAP_GUIDE.md`, `PROJECT_STRUCTURE.md`, `COMMANDS.md`, and `FRAMEWORK_README.md` — are now stale and require synchronization (see Flag 19, below). This housekeeping does not require a Gate event, since it corrects already-`Active` documents' own descriptions rather than introducing new architecture.
 
Architecture Status:
Frozen
 
Blueprint Status:
Completed
 
Implementation Status:
v10.0 — Complete. v10.1 — The Owner-approved pull-forward item (`project-mobile_v01.md`, `DEC-011`) is complete; four documents require a routine synchronization update to reflect it (Flag 19). Remaining v10.1 scope (`AI_DEVELOPMENT_MANUAL.md`, the full Project Template set, the full five-tool Prompt Library) remains deferred and is not authorized for generation without its own, separate Gate 1 decision.
 
---
 
# Completed Milestones
 
- [x] Audit
- [x] Audit Review
- [x] Final Decision Freeze
- [x] Blueprint Input Freeze
- [x] FRAMEWORK_BLUEPRINT.md
- [x] FRAMEWORK_README.md
- [x] global_rules_revisionfinal_v10.md
- [x] PROJECT_BOOTSTRAP_GUIDE.md
- [x] project-pc-app_v04.md
- [x] SKILLS.md (Skill Manifest and Registry — expanded structure: Classification, Metadata Schema, Discovery, Dependencies, Composition, Lifecycle, an informational Multi-Agent Compatibility note, and an explicit, bounded Cross-Project Design Inspiration section. All three manifest rows — `create-feature`, `review-code`, `generate-tests` — are now reconciled to `Active`; see below.)
- [x] DECISIONS.md (seeded — Layer 10 Knowledge Base; retrospectively transcribes 33 individually-evidenced decision identifiers and all 10 corrections from `BLUEPRINT_INPUT_FREEZE.md`, per its own Section 3; explicitly records `TD-001`, `TD-003`, `TD-004`, `TD-005`, and the `LP-` series as a known, honestly-flagged gap per its own Section 4.2, rather than fabricating their content. `DEC-010` and `DEC-011` have since been appended, per PR-001 — see Changelog, below.)
- [x] skill-create-feature.md (Layer 7 — first of the three Tier 1 starter Skill documents; the full seven-field specification of `create-feature`, SK-002. Reconciles `SKILLS.md` Section 12's provisional `gate: none` value for `create-feature` into an authoritative declaration.)
- [x] skill-review-code.md (Layer 7 — second of the three Tier 1 starter Skill documents; the full seven-field specification of `review-code`, SK-002. Reconciles `SKILLS.md` Section 12's already-non-provisional `Gate 4 — Implementation Approval` assignment for `review-code` into this document's own authoritative declaration. Defines a nine-stage review workflow covering all ten required review dimensions — Blueprint, Framework Rule, Architecture, and Technology compliance; Code Quality; Security; Documentation impact; Test coverage; RFC 2119 compliance where applicable; with Maintainability applied as the cross-cutting weighing lens rather than an isolated stage — together with a Critical/Major/Minor Severity Classification that reuses terminology already present elsewhere in this framework's document set rather than introducing new architecture.)
- [x] skill-generate-tests.md (Layer 7 — third and final of the three Tier 1 starter Skill documents; the full seven-field specification of `generate-tests`, SK-002. Reconciles `SKILLS.md` Section 12's provisional `gate: none` value for `generate-tests` into an authoritative declaration. Defines a nine-stage workflow — Intake and Test Scope Analysis, Test Strategy and TDD Boundary Mapping, Unit Test Generation, Integration Test Generation, End-to-End Test Plan Generation, Regression Test Generation, Edge Case and Negative Test Case Generation, Self Review and Documentation Update, and Gate Check and Handoff — producing Unit Tests, Integration Tests, End-to-End Test Plans, Regression Tests, Edge Cases, and Negative Test Cases, organized by an explicitly-scoped Test Category Taxonomy that is a documentation lens over the already-frozen Layer 2 TDD Boundary rather than new architecture. Also formally accommodates the `create-feature` ↔ `generate-tests` dependency-ordering reversal already anticipated by `SKILLS.md`, Section 7. With this document's creation, all four Layer 7 artifacts required by `FRAMEWORK_BLUEPRINT.md` Section 2.7 now exist, and Layer 7 as a whole transitioned to `Active` — see Changelog, 2026-07-19.)
- [x] TEMPLATE_SPEC.md (Layer 9 — the required Project Template Specification, correction C-10, per `FRAMEWORK_BLUEPRINT.md` Sections 2.9 and 12.1. Defines the specification-first pattern for Layer 9 and the five required categories every concrete template must satisfy: (1) required directory-structure conformance to a template's target Layer 4 document and, once `Active`, `PROJECT_STRUCTURE.md`; (2) mandatory documents (`README.md`, `.env.example`, `.gitignore`); (3) mandatory Skill references to the three `Active` Tier 1 Skills via `SKILLS.md`; (4) mandatory Prompt Library references, satisfied in full following Layer 8's completion; and (5) archetype and `PROJECT_STRUCTURE.md` technology-conformance. Includes a full conformance validation procedure (Section 10), an explicit resolution — per correction C-05 — of how this document plus one seed template together satisfy the Layer 9 KA-004 minimum-artifact requirement, and an explicit Cross-Project Design Inspiration section bounding general patterns drawn from Cookiecutter, Copier, and Claude Forge to informational status only, adopting no templating engine, questionnaire mechanism, hook runtime, or update/diff mechanism from any of them. No new architectural decision was introduced; every requirement traces to `FRAMEWORK_BLUEPRINT.md` Sections 2.9 and 12.)
- [x] project-personal-full-stack_v01.md (Layer 4 — Project Rules, Full-Stack Application archetype, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17. Generated out of the originally-listed Next Targets order specifically to resolve a blocking dependency surfaced while attempting `template-fastapi-sqlite/`: per `TEMPLATE_SPEC.md`, Section 14.3, a concrete template cannot be validated against Categories 1 and 5 without an `Active` Layer 4 archetype document to conform to, and FastAPI + SQLite corresponds to the Full-Stack — not Desktop/Electron — archetype. Defines the two-application directory layout (`backend/` FastAPI + `frontend/` SPA), per-language naming conventions (Python `snake_case`/`PascalCase`; TypeScript `kebab-case`/`camelCase`), an API error-handling pattern analogous to `project-pc-app_v04.md`'s IPC pattern applied across a network trust boundary instead of a process boundary, and a persistence architecture confining SQLite access to `backend/src/infrastructure/persistence/sqlite/` behind a Repository seam. Confirms pnpm and Zustand — not npm/yarn or Recoil — as this archetype's Layer 3-derived frontend defaults, and Poetry/uv as its Layer 1-derived backend package-management defaults, directly applicable here unlike in the Node.js-only PC App archetype. Supersedes the deprecated `project-full-stack_v04.md` and companions (DL-002). No new architectural decision was introduced; every rule traces to Layers 1–3 or to this layer's own designated directory/naming responsibility. **This is the Layer 4 archetype `template-fastapi-sqlite/` was ultimately confirmed to target, per `DEC-010`.**)
- [x] project-monolithic_v04.md (Layer 4 — Project Rules, Monolithic Application archetype, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17. Generated directly at the Owner's request, out of this document's previously-listed "Next Targets" order. Third of four planned Layer 4 archetype documents to reach `Active`, after `project-pc-app_v04.md` and `project-personal-full-stack_v01.md`. Defines the Monolithic archetype as a single-deployable web application — one FastAPI process serving both its own HTTP API and a compiled React frontend — explicitly distinguished in its own Archetype Definition section from the Full-Stack archetype's separately-deployable model, so archetype selection between the two remains an explicit human decision rather than an inferred one. Defines a directory layout combining `backend/domain/`, `backend/application/`, `backend/infrastructure/`, and `backend/api/` with a `frontend/` React tree; per-language naming conventions consistent with `project-personal-full-stack_v01.md`'s established pattern; a FastAPI request/error-handling pattern analogous to that document's own API pattern and to `project-pc-app_v04.md`'s IPC pattern; and a persistence architecture confining all SQLModel-based database access to `backend/infrastructure/persistence/sqlite/` behind a Repository seam. Confirms FastAPI, SQLModel, SQLite, and React as this archetype's Layer 3 defaults, and Poetry/uv (backend) plus pnpm (frontend) as its package-management defaults. Supersedes the deprecated `project-monolithic_v03.md` and companions (DL-002). No new architectural decision was introduced; every rule traces to Layers 1–3 or to this layer's own designated directory/naming responsibility. **This archetype was considered, and not selected, for `template-fastapi-sqlite/`, per `DEC-010`; it remains `Active` and fully usable for any Monolithic project.**)
- [x] COMMANDS.md (Layer 5 — Developer Manuals, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.5 and 17. Generated directly at the Owner's request, out of this document's previously-listed "Next Targets" order (it had been listed second, behind `template-fastapi-sqlite/`). Serves as the single canonical command-line reference for every operation Layers 1–4 already authorize: environment/session commands (Section 4, operationalizing the Constitution's Development Environment principle — Clone → Open Project → Docker Compose → Start Development); version control commands (Section 5, operationalizing the Layer 2 Git Workflow Rules); package management commands (Section 6, split by toolchain — Poetry/uv for Python backends, pnpm for Node.js/frontend code, consistent with `FRAMEWORK_README.md`, Section 6, Consequence 2, on npm/yarn); testing commands (Section 7, mapped to the Layer 2 TDD Boundary by zone, without naming a specific test runner); linting/type-checking commands (Section 8, same restraint); and an archetype-specific reference (Section 9) covering the three archetypes that were `Active` at the time (Desktop/Electron, Full-Stack, Monolithic), with the Mobile archetype explicitly deferred per `PROJECT_BOOTSTRAP_GUIDE.md`, Section 5.3 as it then read. **Section 9 now requires a Mobile-archetype subsection, since `project-mobile_v01.md` has since reached `Active` — see Flag 19, below.** No new architectural decision was introduced; every command operationalizes a tool, workflow, or convention already named at Layers 1–3 or confirmed in an Active Layer 4 document.)
- [x] PROJECT_STRUCTURE.md (Layer 5 — Developer Manuals, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.5 and 17. Serves as the single canonical, cross-archetype directory-structure reference: cross-archetype structural invariants (Section 3 — the Clean Architecture Boundary, the TDD Boundary, and the vendor-independence seam, each mapped by archetype rather than restated); the full directory tree of each archetype that was `Active` at the time of generation (Sections 4–6), reproduced verbatim from its own Layer 4 source rather than independently authored; an explicit deferral for the Mobile archetype (Section 7), consistent with `project-mobile_v01.md` being `Pending (Tier 3 / v10.1)` at that time; the mandatory root-level documents every project requires (Section 10, consistent with `TEMPLATE_SPEC.md`, Section 6); and this document's role in both manual-scaffold and Layer 9 template conformance validation (Section 9). Explicitly states that this document is a reproduction, not an independent source — any divergence between its trees and their Layer 4 origin is a defect in this document requiring correction, never a competing decision. **Section 7's deferral is now stale, since `project-mobile_v01.md` has since reached `Active` — see Flag 19, below.** No new architectural decision was introduced; every tree and rule traces to a Layer 2 or Layer 4 document already `Active`. This document's completion resolved the previously-open `PROJECT_STRUCTURE.md`-dependent portion of `TEMPLATE_SPEC.md`'s Categories 1 and 5.)
- [x] CLAUDE_CODE_PROMPTS.md (Layer 8 — Prompt Library, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.8, 11, and 17. This was the **first of the minimum two tool-specific Prompt documents** required for Tier 2 completion of Layer 8 (`FRAMEWORK_BLUEPRINT.md`, Section 11.3). It supplies complete, directly-usable Claude Code invocation wrappers for all three `Active` Tier 1 Skills — `/create-feature`, `/generate-tests`, and `/review-code` — each declaring a `primary-agent-role` matching its wrapped Skill's own (per `FRAMEWORK_BLUEPRINT.md`, Section 11.2), each pointing to its Skill document for `input`/`output`/`steps`/`framework-alignment` content rather than duplicating it, and each expressed as a complete, copyable Claude Code custom-command artifact using a `<context>`/`<instructions>`/`<inputs>`/`<output_format>` structure. Also maps the already-frozen AI Session Initialization Sequence (`FRAMEWORK_README.md`, Section 7) onto Claude Code's project-memory mechanism without altering that sequence, and describes — without defining a new mechanism — how Claude Code's native turn-based interaction model satisfies the positional requirement of a named HITL Gate, consistent with the still-deferred HE-003 pause/resume mechanism. No new architectural decision was introduced; every wrapper is a pointer to an already-`Active` Layer 7 Skill, and Claude Code is named only because Layer 8, and only Layer 8, is permitted to reference a specific tool (`FRAMEWORK_BLUEPRINT.md`, Section 1.4).)
- [x] OPENAI_PROMPTS.md (Layer 8 — Prompt Library, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.8, 11, and 17. The **second of the minimum two tool-specific Prompt documents** required for Tier 2 completion of Layer 8. Supplies complete, directly-usable OpenAI Responses API invocation wrappers for all three `Active` Tier 1 Skills — `create-feature`, `generate-tests`, and `review-code` — each reproducing its wrapped Skill's authoritative `primary-agent-role` and `gate` value from that Skill's own document rather than re-declaring them, and each expressed as a complete, copyable `POST /v1/responses` request artifact, with the `review-code` wrapper additionally using the Responses API's `text.format` (`json_schema`) feature to make its findings report machine-parseable without altering any rule the underlying Skill states. **With this document's creation, Layer 8 (Prompt Library) transitioned to `Active` as a whole**, per `FRAMEWORK_BLUEPRINT.md`, Section 2.8 ("Active once at least two tool-specific Prompt sets exist") and Section 11.3 ("minimum of two distinct AI tools"). Also resolved, in full, `TEMPLATE_SPEC.md`'s previously only partially-satisfiable Category 4 (mandatory Prompt Library references) gap. No new architectural decision was introduced; every wrapper is a pointer to an already-`Active` Layer 7 Skill, and OpenAI is named only because Layer 8, and only Layer 8, is permitted to reference a specific tool.)
- [x] template-fastapi-sqlite/ (Layer 9 — Project Templates, Tier 2, per `FRAMEWORK_BLUEPRINT.md` Sections 2.9 and 12.3. The concrete, conforming seed template `TEMPLATE_SPEC.md`, Section 12, identified as the outstanding artifact required, together with `TEMPLATE_SPEC.md` itself, to satisfy the Layer 9 KA-004 minimum-artifact requirement. Generated directly at the Owner's request, immediately after the Owner resolved the standing archetype-disambiguation question first surfaced under Flag 10: `template-fastapi-sqlite/` conforms to `project-personal-full-stack_v01.md` (Full-Stack), not `project-monolithic_v04.md` (Monolithic) — an explicit Level 1 (Engineering CEO) decision, recorded as `DECISIONS.md` entry `DEC-010`. Validated in full against all five `TEMPLATE_SPEC.md` categories: (1) directory structure — the concrete scaffold reproduces `project-personal-full-stack_v01.md`, Section 4, exactly (`backend/src/{domain,application,infrastructure,api}`, `frontend/src/{components,views,state,api-client}`, mirrored `tests/` trees), and matches `PROJECT_STRUCTURE.md`'s canonical cross-archetype reference; (2) mandatory documents — root `README.md` and `.gitignore`, plus per-application `backend/.env.example` and `frontend/.env.example`, consistent with `PROJECT_STRUCTURE.md`, Section 10's per-application rule for this archetype; (3) mandatory Skill references — the root `README.md` names and points to `SKILLS.md` for all three `Active` Tier 1 Skills (`create-feature`, `generate-tests`, `review-code`); (4) mandatory Prompt Library references — the root `README.md` points to both `Active` tool-specific documents, `CLAUDE_CODE_PROMPTS.md` and `OPENAI_PROMPTS.md`, satisfying the two-tool minimum in full; and (5) archetype/technology conformance — only Layer 3-confirmed defaults are embedded (FastAPI, SQLAlchemy, SQLite, Poetry in `backend/pyproject.toml`; Next.js, TypeScript, Tailwind CSS, Zustand, pnpm in `frontend/package.json`), with the still-unconfirmed test runner, linter, and static type-checker left as an explicit, documented bootstrap-time decision point in both files rather than silently omitted, per `TEMPLATE_SPEC.md`, Section 9, item 2. The scaffold contains no business logic and no feature-specific code beyond a non-domain `/health` liveness endpoint in `backend/src/api/main.py`, consistent with that file's bootstrap-only designation in `project-personal-full-stack_v01.md`, Section 4. **With this artifact's creation, Layer 9 (Project Templates) is `Active` as a whole**, satisfying the KA-004 minimum-artifact requirement (correction C-05) in full — the specification now has its required conforming instance. This was also the final outstanding Tier 2 item across the entire framework: Framework v10 Tier 1 and Tier 2 were, with this artifact, both fully complete. No new architectural decision was introduced by the template's own content; the one genuine architectural decision this milestone depended on — which archetype to target — is recorded separately as `DEC-010`.)
- [x] **project-mobile_v01.md** (Layer 4 — Project Rules, Mobile Application archetype, Tier 3 / v10.1, per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17. Pulled forward ahead of the remainder of v10.1 scope by explicit Owner Gate 1 (Plan Approval) decision, recorded as `DECISIONS.md` entry `DEC-011`. Fourth and final of the four planned Layer 4 archetype documents named in `FRAMEWORK_BLUEPRINT.md`, Section 3, to reach `Active`, after `project-pc-app_v04.md`, `project-personal-full-stack_v01.md`, and `project-monolithic_v04.md`. Maps React Native + Expo's single-JavaScript-runtime model onto the Clean Architecture boundary via a "JavaScript / Native Bridge Boundary" (its own Section 3): domain and application layers as plain, React-Native-agnostic TypeScript with no React Native or Expo imports; infrastructure split into `persistence/sqlite/` (local persistence), `native/` (Expo SDK / native-module adapters — secure storage, biometrics, camera, push notifications), and `network/` (an optional remote backend API client, since a Mobile project MAY operate entirely offline/local-first); and `navigation/`, `screens/`, `components/`, `state/` as presentation-layer delivery code only. Defines a native-bridge-and-network error-handling pattern (its own Section 6) structurally analogous to `project-pc-app_v04.md`'s IPC pattern and the Full-Stack/Monolithic archetypes' API patterns — every native-module call and outbound network call treated as a trust boundary, structured typed errors rather than raw stack traces or native exceptions, no silent swallowing (including denied device permissions), and permissions requested only at the point of actual use, per the least-privilege principle. Confines local SQLite access to `src/infrastructure/persistence/sqlite/` behind a Repository seam, with credential-class data explicitly routed instead to device-native secure storage in `src/infrastructure/native/`, per the Layer 2 Security Baseline. Confirms React Native + Expo (Primary; Flutter named comparison-only, not an approved alternative), TypeScript, pnpm, and Zustand as this archetype's Layer 3 defaults, while deliberately deferring the specific navigation library, SQLite binding, test runner, e2e tool, and build/distribution service (e.g., Expo Application Services) to a Layer 3 lookup at bootstrap time — the same restraint every sibling archetype document already applies to its own undetermined tooling selections. No new architectural decision was introduced beyond what `FRAMEWORK_BLUEPRINT.md`, Section 2.4, assigns to this layer's responsibility, and beyond the single Gate 1 decision (`DEC-011`) that authorized generating this document ahead of the remainder of v10.1 scope. **With this document's creation, all four planned Layer 4 archetype documents are now `Active`, and the `DEC-011` pull-forward's approved scope is fully delivered.** This document's own Section 12 flags three other `Active` documents as now stale, each having pre-declared its own update obligation: `PROJECT_BOOTSTRAP_GUIDE.md` Section 5.3 (currently instructs declining Mobile-archetype bootstrap requests outright), `PROJECT_STRUCTURE.md` Section 7 (currently a deferred placeholder), and `COMMANDS.md` Section 9.4 (currently declines Mobile-archetype command requests) — see Flag 19, below.)
 
---
 
# Current Work
 
Current Target:
 
None — housekeeping only.
 
(Framework v10 Tier 1 and Tier 2 remain fully complete, and the single Owner-approved v10.1 pull-forward item, `project-mobile_v01.md` (`DEC-011`), has now been generated and is `Active`. **All four planned Layer 4 archetype documents — Desktop, Full-Stack, Monolithic, and Mobile — are `Active`.** No further v10.1 scope is currently Gate-1-authorized: `AI_DEVELOPMENT_MANUAL.md`, the remaining Layer 9 template set, and the remaining Layer 8 tool coverage all remain deferred, and per `FRAMEWORK_BLUEPRINT.md`, Section 18.4, generating any of them requires its own, separate Gate 1 (Plan Approval) decision recorded in `DECISIONS.md` — `DEC-011`'s approval does not extend to them. The framework's only active work at this time is a routine, no-Gate-required synchronization pass across four documents whose own text pre-declared they would be updated once `project-mobile_v01.md` reached `Active`: `PROJECT_BOOTSTRAP_GUIDE.md` (Section 5.3, currently declines Mobile bootstrap requests), `PROJECT_STRUCTURE.md` (Section 7, currently a deferred placeholder), `COMMANDS.md` (Section 9.4, currently declines Mobile command requests), and `FRAMEWORK_README.md` (Sections 4.3 and 5, currently list `project-mobile_v01.md` as in-progress rather than `Active`) — see Flag 19, below, for the full detail on each.)
 
Next Targets:
 
1. Synchronization housekeeping (no Gate required) — update `PROJECT_BOOTSTRAP_GUIDE.md`, `PROJECT_STRUCTURE.md`, `COMMANDS.md`, and `FRAMEWORK_README.md` to reflect `project-mobile_v01.md`'s `Active` status, per Flag 19.
2. `AI_DEVELOPMENT_MANUAL.md` (v10.1, still deferred — requires its own, separate Gate 1 decision before generation may begin; not authorized by `DEC-011`)
3. Full Project Template set beyond `template-fastapi-sqlite/` (v10.1, still deferred — same condition as above)
4. Full five-tool Prompt Library beyond Claude Code and OpenAI (v10.1, still deferred — same condition as above)
 
---
 
# Frozen Architecture
 
Architecture is frozen.
 
No architectural redesign is allowed.
 
Only implementation improvements are permitted.
 
If inconsistencies are discovered, classify them as:
 
- Critical
- Major
- Minor
Do not redesign unless explicitly requested by the Owner.
 
---
 
# Framework Principles
 
- Human acts as Engineering CEO.
- Framework is the Engineering Operating System.
- AI performs engineering execution.
- Runtime-independent.
- Vendor-independent.
- RFC 2119 compliant.
---
 
# Active Documents
 
- FRAMEWORK_BLUEPRINT.md
- BLUEPRINT_INPUT_FREEZE.md
- AI_DEVELOPMENT_PHILOSOPHY_v2.0.md
- global_technology_stack_v10.md
- global_rules_revisionfinal_v10.md
- FRAMEWORK_HANDOVER.md
- FRAMEWORK_README.md (Sections 4.3 and 5 currently stale re: `project-mobile_v01.md` — see Flag 19)
- PROJECT_BOOTSTRAP_GUIDE.md (Section 5.3 currently stale re: Mobile bootstrap requests — see Flag 19)
- project-pc-app_v04.md
- SKILLS.md (Skill Manifest and Registry, expanded form incl. Cross-Project Design Inspiration section — all three manifest rows, `create-feature`, `review-code`, and `generate-tests`, are now reconciled to `Active`, per `skill-create-feature.md`, `skill-review-code.md`, and `skill-generate-tests.md` respectively)
- DECISIONS.md (seeded — Layer 10 Knowledge Base; known gap for `TD-001`, `TD-003`, `TD-004`, `TD-005`, and the `LP-` series remains open per its own Section 4.2; `DEC-010` and `DEC-011` have been appended, per PR-001)
- skill-create-feature.md (Layer 7 — first of three Tier 1 starter Skill documents)
- skill-review-code.md (Layer 7 — second of three Tier 1 starter Skill documents)
- skill-generate-tests.md (Layer 7 — third of three Tier 1 starter Skill documents)
- TEMPLATE_SPEC.md (Layer 9 — the required Project Template Specification, correction C-10; together with `template-fastapi-sqlite/`, below, now fully satisfies the Layer 9 KA-004 minimum-artifact requirement per correction C-05.)
- project-personal-full-stack_v01.md (Layer 4 — Project Rules, Full-Stack Application archetype, Tier 2; second of four planned Layer 4 archetype documents to reach `Active`, after `project-pc-app_v04.md`; the archetype `template-fastapi-sqlite/` conforms to, per `DEC-010`)
- project-monolithic_v04.md (Layer 4 — Project Rules, Monolithic Application archetype, Tier 2; third of four planned Layer 4 archetype documents to reach `Active`, after `project-pc-app_v04.md` and `project-personal-full-stack_v01.md`)
- **project-mobile_v01.md** (Layer 4 — Project Rules, Mobile Application archetype, Tier 3 / v10.1, pulled forward per `DEC-011`. Fourth and final of the four planned Layer 4 archetype documents to reach `Active`. Confirms React Native + Expo, TypeScript, pnpm, and Zustand as this archetype's Layer 3 defaults.)
- COMMANDS.md (Layer 5 — Developer Manuals, Tier 2; the canonical command reference for environment/session, version control, package management, testing, and linting/type-checking operations, plus an archetype-specific reference covering the three archetypes `Active` at the time of its own generation — Section 9.4's Mobile deferral is now stale; see Flag 19)
- PROJECT_STRUCTURE.md (Layer 5 — Developer Manuals, Tier 2; the canonical, cross-archetype directory-structure reference — cross-archetype structural invariants, the full directory tree of each archetype `Active` at the time of its own generation reproduced verbatim from its own Layer 4 source, and the mandatory root-level documents every project requires. Resolved the `PROJECT_STRUCTURE.md`-dependent portion of `TEMPLATE_SPEC.md` Categories 1 and 5. Section 7's Mobile deferral is now stale; see Flag 19.)
- CLAUDE_CODE_PROMPTS.md (Layer 8 — Prompt Library, Tier 2; the first of the minimum two required tool-specific Prompt documents. Provides complete Claude Code invocation wrappers for all three `Active` Tier 1 Skills. Together with `OPENAI_PROMPTS.md`, below, completes Layer 8 as a whole.)
- OPENAI_PROMPTS.md (Layer 8 — Prompt Library, Tier 2; the second of the minimum two required tool-specific Prompt documents. Provides complete OpenAI Responses API invocation wrappers for all three `Active` Tier 1 Skills. Completes Layer 8 as a whole.)
- template-fastapi-sqlite/ (Layer 9 — Project Templates, Tier 2; the concrete, conforming seed template targeting `project-personal-full-stack_v01.md` (Full-Stack), per `DEC-010`. Together with `TEMPLATE_SPEC.md`, above, satisfies the Layer 9 KA-004 minimum-artifact requirement in full.)

**Layer 4 (Project Rules) is not, and is not defined as, a single "whole" the way Layer 7 is** — each archetype document (`project-pc-app_v04.md`, `project-personal-full-stack_v01.md`, `project-monolithic_v04.md`, `project-mobile_v01.md`) is independently `Active`, `Pending`, or deferred, per `FRAMEWORK_BLUEPRINT.md`, Section 2.4's "Outputs" list. **All four planned archetype documents are now `Active`.** No further Layer 4 archetype is currently planned (`project-mobile_v01.md`, Section 12, Rule 6).

**Layer 7 (AI Skills) is `Active` as a whole**, per `FRAMEWORK_BLUEPRINT.md`, Section 2.7 ("Active once the manifest and three starter Skills exist"). All four required artifacts — `SKILLS.md`, `skill-create-feature.md`, `skill-review-code.md`, and `skill-generate-tests.md` — exist in complete, `Active` form.
 
**Layer 8 (Prompt Library) is `Active` as a whole.** Per `FRAMEWORK_BLUEPRINT.md`, Section 2.8 ("Active once at least two tool-specific Prompt sets exist") and Section 11.3 ("minimum of two distinct AI tools"), both required tool-specific Prompt documents exist: `CLAUDE_CODE_PROMPTS.md` (Claude Code) and `OPENAI_PROMPTS.md` (OpenAI).
 
**Layer 9 (Project Templates) is `Active` as a whole.** Per `FRAMEWORK_BLUEPRINT.md`, Section 12.3, and correction C-05, `TEMPLATE_SPEC.md` — the required specification — together with `template-fastapi-sqlite/` — the required conforming concrete instance — satisfies the Layer 9 KA-004 minimum-artifact requirement in full. The genuine architectural decision this transition depended on — which archetype `template-fastapi-sqlite/` targets — is recorded separately as `DEC-010`.
 
**Layer 5 (Developer Manuals) has all five of its currently-in-scope v10 planned artifacts `Active`** (`FRAMEWORK_README.md`, `PROJECT_BOOTSTRAP_GUIDE.md`, `CONTRIBUTING.md`, `COMMANDS.md`, and `PROJECT_STRUCTURE.md`); only `AI_DEVELOPMENT_MANUAL.md` (Tier 3 / v10.1, still deferred, not authorized by `DEC-011`) remains outstanding for this layer. Three of the five `Active` artifacts (`PROJECT_BOOTSTRAP_GUIDE.md`, `PROJECT_STRUCTURE.md`, `COMMANDS.md`) currently contain stale content specifically about the Mobile archetype, now that `project-mobile_v01.md` is `Active` — see Flag 19.

**Layer 10 (Knowledge Base) is `Active` and fully current.** `DECISIONS.md` and `FRAMEWORK_HANDOVER.md` both exist and are `Active`. `DEC-010` and `DEC-011` have both been appended to `DECISIONS.md`, per PR-001, with `FRAMEWORK_HANDOVER.md` updated in the same window.

**Layer 11 (Reference Documents) — `V10_MIGRATION_NOTES.md` remains `Deprecated`**, per its own pre-scheduled DL-003 condition, unchanged since the prior revision. Nothing in this update reopens or alters that transition.
 
---
 
# Documents To Generate
 
- AI_DEVELOPMENT_MANUAL.md (v10.1 — deferred; requires its own, separate Gate 1 decision, not authorized by `DEC-011`)
- Full Project Template set beyond `template-fastapi-sqlite/` (v10.1 — deferred; same condition as above)
- Full five-tool Prompt Library beyond Claude Code and OpenAI (v10.1 — deferred; same condition as above)

No document is currently in active generation. The framework's only outstanding work is the synchronization housekeeping named under "Current Work," above, against four documents that already exist and are `Active`.
---
 
# AI Session Instructions
 
When starting a new session:
 
1. Read FRAMEWORK_README.md
2. Read FRAMEWORK_STATUS.md
3. Read FRAMEWORK_BLUEPRINT.md
4. Read DECISIONS.md (if available)
5. Continue implementation.
6. Do not redesign architecture.
7. Update FRAMEWORK_STATUS.md after completing a document.
---
 
# Open Inconsistency Flags
 
**Flag 1 — README/STATUS synchronization gap (Minor, Resolved — see Flag 15).** As of the 2026-07-03 update, the repository copy of `global_rules_revisionfinal_v10.md` existed in full and carried a header status of `Active`, but `FRAMEWORK_README.md` Sections 4.1 and 5 still listed it as "Pending — not yet generated." This flag remains open: this document's edits are confined to `FRAMEWORK_STATUS.md` itself, and `FRAMEWORK_README.md` has still not been updated as part of any change to date. It still requires the Section 4–6 update its own Section 9 mandates ("An out-of-date `FRAMEWORK_README.md` is a framework defect"). Flagged for the Owner rather than silently corrected in `FRAMEWORK_README.md`, since editing that document is outside the scope of STATUS-only updates.
 
**Flag 2 — README/STATUS synchronization gap, project-pc-app_v04.md (Minor, Resolved — see Flag 15).** As of the 2026-07-04 update, `project-pc-app_v04.md` exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.1 still lists it as "Pending — not yet generated," Section 4.6 still names `global_rules_revisionfinal_v10.md` (itself already `Active`, per Flag 1) as the framework's "single next action," and Section 5's domain table still marks the Desktop Application row `Pending`. Same root cause and remedy as Flag 1; flagged for the Owner rather than performed here.
 
**Flag 3 — README/STATUS synchronization gap, SKILLS.md (Minor, closed on substance / superseded by Flag 7).** `SKILLS.md` (the Layer 7 Skill Manifest and Registry) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.1 still lists the "SKILLS.md + 3 starter Skills" bundle as "Pending — not yet generated" in its entirety, and Section 5's domain table still marks "Skill discovery index" (`SKILLS.md`) as `Pending`. This flag covered all three revisions `SKILLS.md` has undergone. As of the 2026-07-19 update, all three manifest rows are `Active`, and Layer 7 as a whole reached `Active` status (see Flag 7, which carries the remediation forward in its final, complete form). This flag is retained for historical traceability rather than deleted, consistent with the append-only discipline this framework applies to its Knowledge Base (`FRAMEWORK_BLUEPRINT.md`, Section 13.2); its substance is fully absorbed into Flag 7.
 
**Flag 4 — README/STATUS synchronization gap, DECISIONS.md (Minor, Resolved — see Flag 15).** `DECISIONS.md` (the Layer 10 Knowledge Base, seeded) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.1 still lists `DECISIONS.md (seeded)` as "Pending — not yet generated," and Section 5's domain table still marks "Architectural decision history" (`DECISIONS.md`) as `Pending`. `DECISIONS.md`'s own closing reminder had already requested this correction; it has still not been reflected in `FRAMEWORK_README.md` itself. Same root cause and remedy as Flags 1–3; flagged for the Owner rather than corrected here, since editing `FRAMEWORK_README.md` is outside the scope of a STATUS-only update. The 2026-07-23 review of `DECISIONS.md` (see Changelog) did not touch `FRAMEWORK_README.md` and does not close this flag.
 
**Flag 5 — README/STATUS synchronization gap, skill-create-feature.md (Minor, superseded by Flag 7).** `skill-create-feature.md` — the first of the three Tier 1 starter Skill documents — exists in full and carries a header status of `Active`. Its generation also reconciled the `create-feature` row of `SKILLS.md` Section 12 from a provisional `gate` value to an authoritative one. This flag's substance is now fully absorbed into Flag 7, since all three starter Skill documents are `Active` and the bundle line item must instead be replaced entirely, not merely refined.
 
**Flag 6 — README/STATUS synchronization gap, skill-review-code.md (Minor, superseded by Flag 7).** `skill-review-code.md` — the second of the three Tier 1 starter Skill documents — exists in full and carries a header status of `Active`. Its generation also reconciled the `review-code` row of `SKILLS.md` Section 12 into a fully authoritative seven-field declaration. This flag's substance is now fully absorbed into Flag 7, below, following the completion of the third starter Skill document.
 
**Flag 7 — README/STATUS synchronization gap, Layer 7 completion (Minor, supersedes Flags 3, 5, and 6; Resolved — see Flag 15).** With `skill-generate-tests.md` `Active` as of the 2026-07-19 update, all four artifacts `FRAMEWORK_BLUEPRINT.md`, Section 2.7, requires for Layer 7 completeness exist: `SKILLS.md` and its three starter Skill documents (`skill-create-feature.md`, `skill-review-code.md`, `skill-generate-tests.md`). **Layer 7 (AI Skills) is therefore `Active` as a whole**, per this document's own "Active Documents" section above. `FRAMEWORK_README.md`, however, still requires correction in at least three places: (a) Section 4.1 still lists the entire "SKILLS.md + 3 starter Skills" line item as a single, undifferentiated "Pending — not yet generated" entry, rather than reflecting that all four constituent artifacts are `Active` and Layer 7 is complete; (b) Section 5's domain table still marks "Skill discovery index" (`SKILLS.md`) and "Individual Skill definitions" (`skill-create-feature.md`, `skill-review-code.md`, `skill-generate-tests.md`) as `Pending`; and (c) Section 4.6's "Current Priority" framing, already stale per Flag 1, should additionally note that Layer 7 no longer represents a gap. This flag supersedes and consolidates Flags 3, 5, and 6, which are retained above for historical traceability only and MUST NOT be treated as separately actionable going forward. Flagged for the Owner rather than corrected here, per the same STATUS-only scope limit as Flags 1–6.
 
**Flag 8 — README/STATUS synchronization gap, TEMPLATE_SPEC.md (Minor, Resolved — see Flag 15).** `TEMPLATE_SPEC.md` (the required Layer 9 Project Template Specification, correction C-10) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.1 still lists `TEMPLATE_SPEC.md` as "Pending — not yet generated" within the Tier 1 table, and Section 5's domain table still marks "Project template structural requirements" (`TEMPLATE_SPEC.md`) as `Pending`. Section 4.6's "Current Priority" framing, already stale per Flag 1 and further stale per Flag 7, does not yet account for `TEMPLATE_SPEC.md`'s completion either. Separately, and not a `FRAMEWORK_README.md` gap: per `TEMPLATE_SPEC.md`'s own Section 12, this document's `Active` status does not by itself complete Layer 9 — a concrete conforming instance (`template-fastapi-sqlite/`) was additionally required to satisfy the KA-004 minimum-artifact requirement; that instance has since been generated (Flag 18). This flag covers only the `FRAMEWORK_README.md` synchronization gap, following the same root cause and remedy as Flags 1–7; it is flagged for the Owner rather than corrected here, since editing `FRAMEWORK_README.md` is outside the scope of a STATUS-only update.
 
**Flag 9 — README/STATUS synchronization gap, project-personal-full-stack_v01.md (Minor, Resolved — see Flag 15).** `project-personal-full-stack_v01.md` (Layer 4 — Project Rules, Full-Stack Application archetype, Tier 2) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.2 still lists it as "Pending — deferred until Tier 1 complete," and Section 5's domain table still marks "Full-stack application structure" as "Pending (Tier 2)." This document was generated out of the Blueprint's nominal Tier 2 sequencing — ahead of `project-monolithic_v04.md` and the remaining Tier 2 items — specifically because `template-fastapi-sqlite/` (Layer 9) could not proceed without an `Active` Layer 4 document to conform to (`TEMPLATE_SPEC.md`, Section 14.3), and FastAPI + SQLite corresponds to the Full-Stack archetype rather than to Desktop/Electron, the only archetype `Active` at the time. This sequencing deviation reflects a dependency the Blueprint's own Section 4 (Dependency Hierarchy: `L4 --> L9`) already implies but which `FRAMEWORK_STATUS.md`'s prior "Next Targets" ordering had not surfaced; it is not a Gate 2 architectural exception, since no rule was bypassed — the document was simply generated earlier in sequence than originally listed, with every one of its own rules still traceable to Layers 1–3 as required. Flagged for the Owner rather than corrected in `FRAMEWORK_README.md` here, following the same root cause and remedy as Flags 1–8.
 
**Flag 10 — README/STATUS synchronization gap, project-monolithic_v04.md, and the archetype-disambiguation question (Minor; README-sync portion Resolved — see Flag 15; archetype-disambiguation core Resolved — see Flag 18).** `project-monolithic_v04.md` (Layer 4 — Project Rules, Monolithic Application archetype, Tier 2) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.2 still lists it as "Pending — deferred until Tier 1 complete," and Section 5's domain table still marks "Monolithic application structure" as "Pending (Tier 2)." Unlike `project-personal-full-stack_v01.md` (Flag 9), this document was generated directly at the Owner's request rather than to resolve a surfaced blocking dependency; Tier 1 was already complete at the time of generation (satisfying DI-002), and no rule was bypassed — the document was simply generated ahead of its position in this STATUS document's own "Next Targets" queue. Its generation surfaced a new, honestly-reportable gap of its own: with both `project-personal-full-stack_v01.md` and `project-monolithic_v04.md` `Active`, and both archetype-appropriate for a FastAPI-based project, which of the two `template-fastapi-sqlite/` (the Layer 9 Current Target at the time) was intended to conform to was no longer self-evident from a single candidate, per `TEMPLATE_SPEC.md`, Section 5, Rule 1. **This question is now resolved: the Owner explicitly confirmed `project-personal-full-stack_v01.md` (Full-Stack) as the target, recorded as `DECISIONS.md` entry `DEC-010` (Flag 18), and `template-fastapi-sqlite/` has been generated against it.** The `FRAMEWORK_README.md` synchronization portion of this flag remains covered by Flag 15's resolution; the archetype-disambiguation core — this flag's substantive reason for being opened — is now fully resolved per Flag 18.
 
**Flag 11 — README/STATUS synchronization gap, COMMANDS.md (Minor, Resolved — see Flag 15).** `COMMANDS.md` (Layer 5 — Developer Manuals, Tier 2) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.2 still lists it as "Pending — deferred until Tier 1 complete," and Section 5's domain table still marks "Canonical command reference" (`COMMANDS.md`) as "Pending (Tier 2)." This document was generated directly at the Owner's request, out of this STATUS document's previously-listed "Next Targets" order (it had been listed second, behind `template-fastapi-sqlite/`); no rule was bypassed, since Tier 1 was already complete and `COMMANDS.md`'s own generation depends only on Layers 1–4, all of which were already `Active` for the three archetypes it documented at the time. `COMMANDS.md` itself explicitly and honestly deferred naming a specific test runner, linter, static type-checker, or Electron packaging/installer tool, consistent with the same restraint every Active Layer 4 document already applies to those identical selections — this is not a gap `COMMANDS.md` introduces, but one it inherits and reports plainly (`COMMANDS.md`, Section 11). Flagged for the Owner rather than corrected in `FRAMEWORK_README.md` here, following the same root cause and remedy as Flags 1–10. **Superseded, for the Mobile-archetype-specific portion, by Flag 19** — `COMMANDS.md`, Section 9.4, now needs a Mobile subsection following `project-mobile_v01.md`'s completion.
 
**Flag 12 — README/STATUS synchronization gap, PROJECT_STRUCTURE.md (Minor, Resolved — see Flag 15).** `PROJECT_STRUCTURE.md` (Layer 5 — Developer Manuals, Tier 2) exists in full and carries a header status of `Active`, but `FRAMEWORK_README.md` Section 4.2 still lists it as "Pending — deferred until Tier 1 complete," and Section 5's domain table still marks "Canonical directory structure reference" (`PROJECT_STRUCTURE.md`) as "Pending (Tier 2)." This document's generation was the natural continuation of the "Next Targets" queue following `COMMANDS.md`, consistent with the Document Generation Order (`FRAMEWORK_BLUEPRINT.md`, Section 17); no rule was bypassed. `PROJECT_STRUCTURE.md` is a reproduction of directory trees already fixed in the Layer 4 documents `Active` at the time, organized into a single cross-archetype reference and a set of cross-archetype structural-invariant tables (its own Section 3), and explicitly states that its own reproductions are non-authoritative relative to their Layer 4 sources (its own Section 14) — no new architectural decision was introduced. Its completion resolved the `PROJECT_STRUCTURE.md`-dependent portion of `TEMPLATE_SPEC.md` Categories 1 and 5 but did **not** resolve the Flag 10 archetype-disambiguation question, which was resolved separately (Flag 18). Flagged for the Owner rather than corrected in `FRAMEWORK_README.md` here, following the same root cause and remedy as Flags 1–11. **Superseded, for the Mobile-archetype-specific portion, by Flag 19** — `PROJECT_STRUCTURE.md`, Section 7, now needs the Mobile directory tree reproduced, following `project-mobile_v01.md`'s completion.
 
**Flag 13 — README/STATUS synchronization gap, CLAUDE_CODE_PROMPTS.md, and a partial-completion nuance for Layer 8 (Minor, closed on substance / superseded by Flag 14).** `CLAUDE_CODE_PROMPTS.md` (Layer 8 — Prompt Library, Tier 2) exists in full and carries a header status of `Active`, but at the time this flag was opened, `FRAMEWORK_README.md` and `TEMPLATE_SPEC.md` both understated Layer 8's progress. As of the 2026-07-27 (2) update, with `OPENAI_PROMPTS.md` `Active` as the second required tool-specific document, this flag's substance is fully absorbed into Flag 14, below. Retained for historical traceability only, consistent with the append-only discipline this framework applies to its Knowledge Base (`FRAMEWORK_BLUEPRINT.md`, Section 13.2); MUST NOT be treated as separately actionable going forward.
 
**Flag 14 — README/STATUS synchronization gap, OPENAI_PROMPTS.md, and Layer 8 completion (Minor, supersedes Flag 13; Resolved — see Flag 15).** With `OPENAI_PROMPTS.md` `Active` as of the 2026-07-27 (2) update, both artifacts `FRAMEWORK_BLUEPRINT.md`, Sections 2.8 and 11.3, require for Layer 8 completeness exist. **Layer 8 (Prompt Library) is therefore `Active` as a whole**, per this document's own "Active Documents" section above. `FRAMEWORK_README.md` required correction in at least two places at the time; `TEMPLATE_SPEC.md`, Section 14.1, required a further correction of its own — both since addressed by the separate `FRAMEWORK_README.md` regeneration recorded in Flag 15. This flag supersedes and consolidates Flag 13, which is retained above for historical traceability only.

**Flag 15 — Retroactive closure of Flags 1, 2, 4, 7, 8, 9, 11, 12, and 14; narrowing of Flag 10 (Minor, Resolved).** `FRAMEWORK_README.md` and `DECISIONS.md` were separately regenerated, outside the scope of this document's own edit history, to bring `FRAMEWORK_README.md` Sections 4–7 into agreement with the framework's actual state. `FRAMEWORK_README.md`'s own Section 9 states explicitly: "This revision itself is an exercise of Rule 2: it brings Sections 4–7 back into agreement with the framework's actual, current state — resolving the accumulated synchronization gaps `FRAMEWORK_STATUS.md` had been tracking as Flags 1, 2, 4, 7, 8, 9, 11, 12, and 14, and narrowing Flag 10 to the single, explicit open question stated in Section 4.6." `DECISIONS.md`'s own closing reminder independently confirms the same set. This document's own Open Inconsistency Flags section had not yet been updated to reflect that regeneration, which is itself the precise class of staleness this section exists to catch — this entry closes that gap. Flags 1, 2, 4, 7, 8, 9, 11, 12, and 14 are accordingly annotated above as **Resolved**, and Flag 10 is annotated as resolved with respect to its `FRAMEWORK_README.md` synchronization portion only; Flag 10's substantive core is closed separately by Flag 18. Per the append-only discipline this framework applies to its Knowledge Base (`FRAMEWORK_BLUEPRINT.md`, Section 13.2), each superseded flag's original body is retained verbatim above rather than deleted.

**Flag 16 — Release Audit findings, five-document synchronization defects (Critical: C-1, C-2, C-3; Major: M-1, M-2; Resolved).** A Release Candidate audit of Framework v10 found that five documents — `SKILLS.md`, `TEMPLATE_SPEC.md`, `PROJECT_BOOTSTRAP_GUIDE.md`, `COMMANDS.md`, and `PROJECT_STRUCTURE.md` — each carried self-referential "Current Applicability at Framework v10's Mid-Migration State" content that had not been updated to reflect the `Active` status of documents each of those sections itself names. All five documents were corrected in this same change: every `Pending` reference to a document that is now `Active` was updated to `Active`, and every conclusion drawn from an outdated status was corrected to match the framework's actual current state. No rule, gate, schema field, or architectural decision was added, changed, or removed in any of the five documents — every edit was a status-synchronization correction only. Genuinely still-open gaps within these documents (the Mobile archetype's Tier 3/v10.1 deferral; the then-open Flag 10 archetype-disambiguation question; the two Layer-3 tool-name lookups in `COMMANDS.md`) were deliberately left open at the time, since they were accurate reports of a real, unresolved state rather than synchronization defects. The Mobile-archetype deferral referenced here has since been resolved by `project-mobile_v01.md`'s completion (Flag 19); the Flag 10 question has since been resolved (Flag 18).

**Flag 17 — Process gap: this section's own tracking blind spot (Minor, acknowledged and addressed by the 2026-07-30 update).** Flags 1 through 14 tracked `FRAMEWORK_README.md` synchronization gaps exclusively. They did not track the parallel, equally real synchronization gaps that accumulated in `SKILLS.md`, `TEMPLATE_SPEC.md`, `PROJECT_BOOTSTRAP_GUIDE.md`, and `COMMANDS.md` (Flag 16) — meaning this document's own audit mechanism had a blind spot for self-referential staleness in any document other than `FRAMEWORK_README.md`. Going forward, an Owner-initiated or agent-initiated Release Audit SHOULD check every Tier 1/2 document's own "Current Applicability" or equivalent self-descriptive section against the framework's actual current state, not `FRAMEWORK_README.md` alone, and SHOULD open a Flag entry here for any document found to have drifted, regardless of which document it is. **Flag 19, below, is a direct application of this rule** — it was opened proactively, at the moment `project-mobile_v01.md` reached `Active`, rather than discovered later by a separate audit pass.

**Flag 18 — `template-fastapi-sqlite/` generated; Flag 10 substantively resolved; Layer 9 and v10 Tier 1+2 completion; `DEC-010`/`DEC-011` recorded; Layer 11 DL-003 deprecation triggered (Resolved — all items closed as of this update).** In the window this flag covers:

1. **Archetype disambiguation resolved.** The Owner explicitly confirmed that `template-fastapi-sqlite/` targets `project-personal-full-stack_v01.md` (Full-Stack), not `project-monolithic_v04.md` (Monolithic) — recorded as `DECISIONS.md` entry `DEC-010`. This is the substantive core of Flag 10, closed as of this decision.
2. **`template-fastapi-sqlite/` generated and validated**, satisfying Layer 9's KA-004 minimum-artifact requirement in full and completing Framework v10 Tier 1 and Tier 2.
3. **`V10_MIGRATION_NOTES.md`'s DL-003 transition condition was met** and it transitioned from `Active` to `Deprecated` — a status observation, not a new decision.
4. **The Owner approved pulling `project-mobile_v01.md` forward from v10.1**, recorded as `DECISIONS.md` entry `DEC-011`.
5. `FRAMEWORK_README.md` was updated to reflect items 1–4.

All five items are now closed: `DEC-010` and `DEC-011` have both been appended to `DECISIONS.md`, `FRAMEWORK_README.md` and `FRAMEWORK_HANDOVER.md` have both been regenerated to reflect the resulting state, and `project-mobile_v01.md` — the subject of `DEC-011` — has itself now been generated (Flag 19, below, tracks the follow-on synchronization this completion requires).

**Flag 19 — `project-mobile_v01.md` generated; `DEC-011` pull-forward complete; four documents now stale (Minor — routine synchronization pattern, no Gate event required; open pending edits to the four named files).** `project-mobile_v01.md` (Layer 4 — Project Rules, Mobile Application archetype) was generated per the Owner's Gate 1 (Plan Approval) decision `DEC-011`, completing that decision's approved scope in full. This is the fourth and final of the four planned Layer 4 archetype documents (`FRAMEWORK_BLUEPRINT.md`, Section 3) — **all four are now `Active`.**

This document's own Section 12 (Change Control) explicitly names three other framework documents as requiring a follow-up update in the same change, each having pre-declared exactly this obligation in its own text:

1. **`PROJECT_BOOTSTRAP_GUIDE.md`, Section 5.3** — currently states: "A Mobile-archetype bootstrap request at this stage MUST be declined at the framework level, not merely deferred, until v10.1 scope begins." This instruction is now stale; a Mobile-archetype bootstrap request MUST instead route through the ordinary Project Creation Flow (`PROJECT_BOOTSTRAP_GUIDE.md`, Section 4), using `project-mobile_v01.md` at Step 3 (Section 4.3) exactly as the other three archetype documents are already used.
2. **`PROJECT_STRUCTURE.md`, Section 7** — currently a deferred placeholder stating that no canonical directory structure is defined for this archetype "since no authoritative Layer 4 source yet exists." `project-mobile_v01.md`, Section 4, now supplies that source; `PROJECT_STRUCTURE.md`, Section 7, MUST be replaced with the reproduced directory tree, per that document's own Section 15, Rule 3.
3. **`COMMANDS.md`, Section 9.4** — currently declines Mobile-archetype command requests, citing consistency with `PROJECT_BOOTSTRAP_GUIDE.md`, Section 5.3. With that underlying instruction now stale (item 1, above), `COMMANDS.md`, Section 9, MUST be extended with a Mobile-archetype subsection analogous to Sections 9.1–9.3, per that document's own Section 15, Rule 4.

A fourth document requires the same *routine* synchronization update every prior Tier 1/2/3 completion has already required, per the established Flags 1–14 pattern:

4. **`FRAMEWORK_README.md`, Sections 4.3 and 5** — currently lists `project-mobile_v01.md` as "In progress — approved pull-forward" rather than `Active`, and Section 5's domain table marks "Mobile application structure" as `Pending`.

**Separately, and not itself a synchronization gap:** with `DEC-011`'s approved scope now fully delivered, no further v10.1 item is currently Gate-1-authorized. `AI_DEVELOPMENT_MANUAL.md`, the remaining Layer 9 template set, and the remaining Layer 8 tool coverage all remain deferred; per `FRAMEWORK_BLUEPRINT.md`, Section 18.4, generating any of them would require its own, separate Gate 1 (Plan Approval) decision recorded in `DECISIONS.md`, not a continuation of `DEC-011`.

Items 1–4 above are Minor, consistent with the ordinary synchronization pattern of Flags 1–14 and 18 — each is a status-currency correction to an already-`Active` document, not a missing document or an unresolved architectural question.
 
---
 
# Changelog

2026-08-03 (2)

**New milestone — `project-mobile_v01.md`, and completion of the `DEC-011` pull-forward scope.** `project-mobile_v01.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17 (Layer 4 — Project Rules, Mobile Application archetype), the single v10.1 item the Owner explicitly approved pulling forward via the Gate 1 (Plan Approval) decision recorded as `DECISIONS.md` entry `DEC-011`. It is the fourth and final of the four planned Layer 4 archetype documents named in `FRAMEWORK_BLUEPRINT.md`, Section 3.

The document maps React Native + Expo's single-JavaScript-runtime model onto the Clean Architecture boundary via a "JavaScript / Native Bridge Boundary" (its own Section 3): domain and application layers as plain, React-Native-agnostic TypeScript with no React Native or Expo imports; infrastructure split into `persistence/sqlite/` (local persistence), `native/` (Expo SDK / native-module adapters — secure storage, biometrics, camera, push notifications), and `network/` (an optional remote backend API client); and `navigation/`, `screens/`, `components/`, `state/` as presentation-layer delivery code only. It defines a native-bridge-and-network error-handling pattern (its own Section 6) structurally analogous to `project-pc-app_v04.md`'s IPC pattern and the Full-Stack/Monolithic archetypes' API patterns — every native-module call and outbound network call treated as a trust boundary, structured typed errors rather than raw stack traces, no silent swallowing, and device permissions requested only at the point of actual use. Its persistence architecture confines local SQLite access to `src/infrastructure/persistence/sqlite/` behind a Repository seam, with credential-class data explicitly routed instead to device-native secure storage in `src/infrastructure/native/`, per the Layer 2 Security Baseline. It confirms React Native + Expo (Primary; Flutter named comparison-only, not an approved alternative), TypeScript, pnpm, and Zustand as this archetype's Layer 3 defaults, while deliberately deferring the specific navigation library, SQLite binding, test runner, e2e tool, and build/distribution service to a Layer 3 lookup at bootstrap time — the same restraint every sibling archetype document already applies to its own undetermined tooling selections.

No architectural decision was introduced beyond what `FRAMEWORK_BLUEPRINT.md`, Section 2.4, assigns to this layer's responsibility, and beyond the single Gate 1 decision (`DEC-011`) that authorized generating this document ahead of the remainder of v10.1 scope.

**With this document's creation, all four planned Layer 4 archetype documents are now `Active`** — Desktop (`project-pc-app_v04.md`), Full-Stack (`project-personal-full-stack_v01.md`), Monolithic (`project-monolithic_v04.md`), and Mobile (`project-mobile_v01.md`) — and **the `DEC-011` pull-forward's approved scope is fully delivered.** No further v10.1 item may be pulled forward without its own, separate Gate 1 decision, per `FRAMEWORK_BLUEPRINT.md`, Section 18.4; `AI_DEVELOPMENT_MANUAL.md`, the remaining Layer 9 template set, and the remaining Layer 8 tool coverage all remain deferred and untouched by `DEC-011`.

This document's own Section 12 (Change Control) explicitly flags three other `Active` documents as now stale, each having promised its own update "in the same change" this event occurred: `PROJECT_BOOTSTRAP_GUIDE.md`, Section 5.3 (currently instructs declining any Mobile-archetype bootstrap request outright); `PROJECT_STRUCTURE.md`, Section 7 (currently a deferred placeholder pending exactly this transition); and `COMMANDS.md`, Section 9.4 (currently declines Mobile-archetype command requests). `FRAMEWORK_README.md`, Sections 4.3 and 5, requires the same ordinary synchronization update this document's own history already applies to every prior Tier 1/2/3 completion.

Moved `project-mobile_v01.md` to "Completed Milestones" and "Active Documents." Updated the Layer 4 status paragraph under "Active Documents" to record all four archetypes `Active`. Current Target changed from `project-mobile_v01.md` to "None — housekeeping only," since no further v10.1 generation is currently Gate-1-authorized. "Next Targets" and "Documents To Generate" updated to match — the three remaining v10.1 items are restated as requiring their own separate Gate 1 event, not as an ordered queue implying they are already approved. Added Flag 19 to "Open Inconsistency Flags."

2026-08-03 (1)

**New milestone — `template-fastapi-sqlite/`, Flag 10 resolved, Layer 9 completion, and Framework v10 (Tier 1 + Tier 2) fully complete.** In response to a direct question, the Owner confirmed `project-personal-full-stack_v01.md` (Full-Stack) as the archetype `template-fastapi-sqlite/` targets — resolving the archetype-disambiguation question first surfaced under Flag 10 (`FRAMEWORK_STATUS.md`, 2026-07-22 entry, below). This is a genuine architectural decision (which of two valid Layer 4 archetypes to conform to), recorded in `DECISIONS.md` as entry `DEC-010`.

`template-fastapi-sqlite/` was then generated as a concrete directory scaffold — not a specification document — conforming in full to `project-personal-full-stack_v01.md`, Section 4, and validated against all five `TEMPLATE_SPEC.md` categories: directory structure (verified against both the target Layer 4 document and `PROJECT_STRUCTURE.md`); mandatory documents (root `README.md` and `.gitignore`, per-application `backend/.env.example` and `frontend/.env.example`); mandatory Skill references (`README.md` points to `SKILLS.md` for all three `Active` Tier 1 Skills); mandatory Prompt Library references (`README.md` points to both `Active` tool-specific documents, `CLAUDE_CODE_PROMPTS.md` and `OPENAI_PROMPTS.md`); and archetype/technology conformance (only Layer 3-confirmed defaults embedded in `backend/pyproject.toml` and `frontend/package.json` — FastAPI, SQLAlchemy, SQLite, Poetry, Next.js, TypeScript, Tailwind CSS, Zustand, pnpm — with the still-unconfirmed test runner, linter, and type-checker left as an explicit, documented decision point rather than silently omitted). The scaffold introduces no business logic and no feature-specific code beyond a non-domain `/health` liveness check in `backend/src/api/main.py`, consistent with that file's bootstrap-only designation in the target Layer 4 document.

**With this artifact's creation, Layer 9 (Project Templates) is `Active` as a whole**, per `FRAMEWORK_BLUEPRINT.md`, Section 12.3, and correction C-05 — the specification (`TEMPLATE_SPEC.md`) now has its required conforming instance. **This was also the framework's final outstanding Tier 2 item: Framework v10 Tier 1 and Tier 2 are both now fully complete**, with every governing layer (1–6) and every v10-scoped operational layer (7, 8, 9, 10) `Active` as a whole.

Completing Tier 1 and Tier 2 in full also mechanically triggers a second, separate status transition: per `FRAMEWORK_BLUEPRINT.md`, Sections 2.11 and 7 (DL-003), **`V10_MIGRATION_NOTES.md` (Layer 11) transitions from `Active` to `Deprecated`**, since its stated condition — "remains `Active` until every Tier 1 and Tier 2 v10 document exists" — is now satisfied. This is a status observation mechanically determined by an already-frozen, pre-scheduled condition, not a new architectural decision, in the same manner Layer 7's and Layer 8's earlier transitions to `Active` were not new decisions.

With v10 complete, the Owner separately approved pulling `project-mobile_v01.md` (Layer 4, Mobile archetype — Tier 3 / v10.1) forward, ahead of the remainder of v10.1 scope, which remains deferred (`AI_DEVELOPMENT_MANUAL.md`; the full Project Template set beyond `template-fastapi-sqlite/`; the full five-tool Prompt Library beyond Claude Code and OpenAI). This is a Gate 1 (Plan Approval) event under `FRAMEWORK_BLUEPRINT.md`, Section 18.4, recorded in `DECISIONS.md` as entry `DEC-011`.

Moved `template-fastapi-sqlite/` to "Completed Milestones" and "Active Documents." Updated the Layer 9, Layer 10, and Layer 11 status paragraphs under "Active Documents" accordingly. Current Target advanced to `project-mobile_v01.md`; Next Targets re-ordered to list it first (in progress, approved pull-forward), followed by the three remaining v10.1 items, all still deferred. "Documents To Generate" updated to match.

Added Flag 18 to "Open Inconsistency Flags," recording: the resolution of Flag 10's substantive core; the appending of `DEC-010` and `DEC-011` to `DECISIONS.md`; the DL-003-triggered deprecation of `V10_MIGRATION_NOTES.md`; and the broader `FRAMEWORK_README.md` update needed to reflect that v10 as a whole — not merely an individual document — had reached completion. Flag 10's header was updated to point to Flag 18 for its final resolution, per the same treatment already applied when Flags 3/5/6 were absorbed into Flag 7 and Flag 13 into Flag 14.
 
2026-07-30

**Release Candidate audit and five-document synchronization pass.** A Release Candidate audit of Framework v10 was performed, reviewing the entire document set for Blueprint compliance, cross-document consistency, broken references, RFC 2119 usage, Mermaid syntax, layer authority, release readiness, migration status consistency, document status synchronization, and naming consistency.

The audit found three Critical and two Major findings, all of the same underlying class: `SKILLS.md`, `TEMPLATE_SPEC.md`, `PROJECT_BOOTSTRAP_GUIDE.md`, `COMMANDS.md`, and `PROJECT_STRUCTURE.md` each carried a self-referential "Current Applicability at Framework v10's Mid-Migration State" section that had not been updated in step with the `Active` status of documents each of those sections itself names. Full detail is recorded in Flag 16, above.

All five documents were corrected in this same change: every `Pending` reference to a document that is now `Active` was updated to `Active`, and every conclusion drawn from an outdated status was corrected to match the framework's actual current state. No rule, gate, schema field, or architectural decision was added, changed, or removed in any of the five documents — every edit was a status-synchronization correction only.

The audit separately found that `FRAMEWORK_README.md` and `DECISIONS.md` had, at some point prior to this audit, already been regenerated (outside the scope of this document's own tracked edit history) to resolve the gaps this document had been tracking as Flags 1, 2, 4, 7, 8, 9, 11, 12, and 14, and to narrow Flag 10 to its Section 4.6 archetype-disambiguation core. This is retroactively closed via Flag 15, above.

Added Flags 15, 16, and 17 to "Open Inconsistency Flags." "Current Target," "Next Targets," "Completed Milestones," and "Active Documents" were unchanged in substance by this pass: no document's actual `Active`/`Pending` status changed, only five documents' own descriptions of already-`Active` statuses were corrected, and `template-fastapi-sqlite/` remained the framework's sole outstanding Tier 2 target, blocked solely on the Flag 10 archetype-disambiguation decision — resolved above, in the 2026-08-03 (1) entry.
 
2026-07-27 (2)
 
**New milestone — `OPENAI_PROMPTS.md`, and Layer 8 completion.** `OPENAI_PROMPTS.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.8, 11, and 17 (Layer 8 — Prompt Library, Tier 2), directly at the Owner's request, out of this document's previously-listed "Next Targets" order. The document provides complete, directly-usable OpenAI Responses API (`POST /v1/responses`) invocation wrappers for all three `Active` Tier 1 Skills, each reproducing its wrapped Skill's authoritative `primary-agent-role` and `gate` value exactly as that Skill's own document declares them. **With this document's creation, Layer 8 (Prompt Library) transitioned to `Active` as a whole.**

Moved to "Completed Milestones" and "Active Documents." Current Target remained `template-fastapi-sqlite/`. Added Flag 14 to "Open Inconsistency Flags," documenting required `FRAMEWORK_README.md` and `TEMPLATE_SPEC.md` corrections. Marked Flag 13 as superseded by Flag 14.
 
2026-07-27 (1)
 
**New milestone — `CLAUDE_CODE_PROMPTS.md`, and Layer 8's first tool-specific document.** `CLAUDE_CODE_PROMPTS.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.8, 11, and 17 (Layer 8 — Prompt Library, Tier 2), directly at the Owner's request, out of this document's previously-listed "Next Targets" order. The document provides complete, directly-usable Claude Code invocation wrappers for all three `Active` Tier 1 Skills. This document did **not**, by itself, bring Layer 8 to `Active` status — a second, distinct tool's equivalent document remained required (see the 2026-07-27 (2) entry, above).

Moved to "Completed Milestones" and "Active Documents." Current Target remained `template-fastapi-sqlite/`. Added Flag 13 to "Open Inconsistency Flags." Flag 13 was subsequently marked superseded by Flag 14 upon `OPENAI_PROMPTS.md`'s completion of Layer 8.
 
2026-07-25
 
**New milestone — `PROJECT_STRUCTURE.md`.** `PROJECT_STRUCTURE.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.5 and 17 (Layer 5 — Developer Manuals, Tier 2), continuing this document's "Next Targets" queue in nominal order following `COMMANDS.md`. The document consolidates, for the first time in a single artifact, the canonical directory structure of every archetype `Active` at the time, together with three cross-archetype structural-invariant tables (the Clean Architecture Boundary, the TDD Boundary, and the vendor-independence seam, each mapped by archetype).

Moved to "Completed Milestones" and "Active Documents." **Layer 5 (Developer Manuals) now has all five of its currently-in-scope planned artifacts `Active`.** Current Target remained `template-fastapi-sqlite/`, with the `PROJECT_STRUCTURE.md`-dependent portion of `TEMPLATE_SPEC.md` Categories 1 and 5 resolved. Added Flag 12 to "Open Inconsistency Flags."
 
2026-07-23 (2)
 
**New milestone — `COMMANDS.md`.** `COMMANDS.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.5 and 17 (Layer 5 — Developer Manuals, Tier 2), directly at the Owner's request, out of this document's previously-listed "Next Targets" order. The document defines environment/session, version control, package management, testing, and linting/type-checking commands, plus an archetype-specific reference covering the three archetypes `Active` at the time.

Moved to "Completed Milestones" and "Active Documents." **Layer 5 (Developer Manuals) now has five of its six planned artifacts `Active`.** Current Target remained `template-fastapi-sqlite/`. Added Flag 11 to "Open Inconsistency Flags."
 
2026-07-23 (1)
 
**Review — `DECISIONS.md` reconfirmed; no new entry, no artifact-state change.** At the Owner's request, `DECISIONS.md` was reviewed against `FRAMEWORK_BLUEPRINT.md`, Sections 2.10 and 13, following a proposed addition describing `template-fastapi-sqlite/` as deferred from v10.0 to v10.1. That framing was declined and not incorporated, since no such decision had in fact been made through a Gate 2 event. Having reviewed `DECISIONS.md` in full, no legitimate basis was found for a new entry at that time. `DECISIONS.md` was regenerated with content identical to its existing `Active` version, recorded here as a verification action.

The Flag 10 archetype-disambiguation question remained open at this point; it was neither raised nor resolved by this review, which was scoped only to `DECISIONS.md`. It has since been resolved — see the 2026-08-03 (1) entry, above.
 
2026-07-22
 
**New milestone — `project-monolithic_v04.md`.** `project-monolithic_v04.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17 (Layer 4 — Project Rules, Monolithic Application archetype, Tier 2), at the Owner's direct request. It is the third of four planned Layer 4 archetype documents to reach `Active`, following `project-pc-app_v04.md` and `project-personal-full-stack_v01.md`.

Moved to "Completed Milestones" and "Active Documents." **Layer 4 (Project Rules) now has three of its four planned archetype documents `Active`.** Current Target remained `template-fastapi-sqlite/`. It was newly noted as having **two** `Active`, FastAPI-capable Layer 4 archetype documents it could plausibly conform to (Full-Stack and Monolithic) — this ambiguity was resolved by explicit Owner decision on 2026-08-03, above. Added Flag 10 to "Open Inconsistency Flags."
 
2026-07-21
 
**New milestone — `project-personal-full-stack_v01.md`, generated ahead of its nominal Tier 2 sequence position.** `project-personal-full-stack_v01.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17 (Layer 4 — Project Rules, Full-Stack Application archetype, Tier 2). Generation was triggered while attempting the in-progress Current Target, `template-fastapi-sqlite/` (Layer 9): per `TEMPLATE_SPEC.md`, Section 5, Rule 1, and Section 14.3, a concrete template's directory structure must conform to an `Active` Layer 4 archetype document, and FastAPI + SQLite corresponds to the Full-Stack or Monolithic archetype, not Desktop/Electron (the only archetype `Active` at the time).

Moved to "Completed Milestones" and "Active Documents." Current Target remained `template-fastapi-sqlite/`, now unblocked for `TEMPLATE_SPEC.md` Categories 1 and 5 against this new document. Added Flag 9 to "Open Inconsistency Flags."
 
2026-07-20
 
**New milestone — `TEMPLATE_SPEC.md`.** `TEMPLATE_SPEC.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.9 and 12 (Layer 9 — Project Templates, the required specification-first parent artifact, correction C-10). It defines the five required content categories every concrete Layer 9 template must satisfy, together with a full conformance validation procedure and an explicit, bounded Cross-Project Design Inspiration section.

Moved to "Completed Milestones" and "Active Documents," with the explicit qualification that Layer 9 as a whole remained incomplete pending a concrete conforming instance. Current Target advanced to `template-fastapi-sqlite/` (Layer 9, Tier 2). Added Flag 8 to "Open Inconsistency Flags."
 
2026-07-19
 
**New milestone — `skill-generate-tests.md`, and Layer 7 completion.** `skill-generate-tests.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.7 and 10 (Layer 7 — AI Skills, full seven-field Skill specification, SK-002). It is the third and final of the three Tier 1 starter Skill documents (SK-004). Its `steps` field defines a nine-stage workflow producing Unit Tests, Integration Tests, End-to-End Test Plans, Regression Tests, Edge Cases, and Negative Test Cases.

Moved to "Completed Milestones" and "Active Documents." With this document's creation, all four artifacts `FRAMEWORK_BLUEPRINT.md`, Section 2.7, requires for Layer 7 completeness now existed, and Layer 7 (AI Skills) was recorded as `Active` as a whole for the first time.

Current Target advanced to `TEMPLATE_SPEC.md` (Layer 9), the next available Tier 1 item per the Document Generation Order. Opened Flag 7 in "Open Inconsistency Flags," consolidating Flags 3, 5, and 6.
 
2026-07-18
 
**New milestone — `skill-review-code.md`.** `skill-review-code.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.7 and 10 (Layer 7 — AI Skills, full seven-field Skill specification, SK-002). It is the second of the three Tier 1 starter Skill documents (SK-004) to be authored. Its `steps` field defines a nine-stage workflow covering all ten required review dimensions, together with a Critical/Major/Minor Severity Classification.

Moved to "Completed Milestones" and "Active Documents," with the explicit qualification that Layer 7 as a whole remained incomplete. Current Target advanced to the sole remaining starter Skill document, `skill-generate-tests.md`. Added Flag 6 to "Open Inconsistency Flags."
 
2026-07-09
 
Two updates recorded in this pass.

**(a) Synchronization correction — `DECISIONS.md`.** `DECISIONS.md` (Layer 10 Knowledge Base, seeded) was found to already exist in the repository in complete, `Active` form. Moved to "Completed Milestones" and "Active Documents." Added Flag 4 to "Open Inconsistency Flags."

**(b) New milestone — `skill-create-feature.md`.** `skill-create-feature.md` was generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.7 and 10 (Layer 7 — AI Skills, full seven-field Skill specification, SK-002). It is the first of the three Tier 1 starter Skill documents (SK-004) to be authored. Its `steps` field defines a seven-stage workflow — Requirement Analysis, Specification, Implementation Planning, Implementation, Self Review, Documentation Update, and Gate Check.

Moved to "Completed Milestones" and "Active Documents," with the explicit qualification that Layer 7 as a whole remained incomplete. Current Target advanced to the two remaining starter Skill documents. Added Flag 5 to "Open Inconsistency Flags."
 
2026-07-07 (3)
 
`SKILLS.md` revised again, superseding the same-day expanded-Registry version described below, per Owner instruction to add an explicit, auditable "Cross-Project Design Inspiration" section. No fact about any Skill's role, gate, metadata, or status changed. Flag 3 updated to explicitly cover all three revisions to date.
 
2026-07-07 (2)
 
`SKILLS.md` revised and expanded, superseding the same-day manifest-only version described below, per Owner instruction to restructure it as a full Layer 7 Skill Manifest and Registry suited to an AI Engineering Operating System. No new Skill, Gate, role, or schema field was introduced. Active Documents and Completed Milestones entries for `SKILLS.md` updated to note the expanded structure.
 
2026-07-07 (1)
 
`SKILLS.md` generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.7 and 10 (Layer 7 — AI Skills, Skill Manifest, Tier 1). This is the manifest half only of the bundled "SKILLS.md + 3 starter Skills" Tier 1 deliverable; moved to "Completed Milestones" and "Active Documents" with that qualification noted explicitly. Current Target advanced to the three individual Skill documents. Added Flag 3 to "Open Inconsistency Flags."
 
2026-07-04
 
`project-pc-app_v04.md` generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.4 and 17 (Layer 4 — Project Rules, Desktop Application / Electron archetype, Tier 1). Moved from "Documents To Generate" to "Active Documents" and "Completed Milestones." Current Target advanced to `DECISIONS.md` (seeded). Added Flag 2 to "Open Inconsistency Flags."
 
2026-07-03
 
`PROJECT_BOOTSTRAP_GUIDE.md` generated per `FRAMEWORK_BLUEPRINT.md` Sections 2.5 and 16 (Layer 5, Tier 1). Moved from "Documents To Generate" to "Active Documents" and "Completed Milestones." Separately, `global_rules_revisionfinal_v10.md` was found to already exist in the repository in complete, `Active` form; moved to "Completed Milestones" and "Active Documents" in this update. Current Target advanced to `project-pc-app_v04.md` (Layer 4).
 
2026-07-01
 
FRAMEWORK_README.md generated per FRAMEWORK_BLUEPRINT.md Sections 14 and 17 (Layer 5, Tier 1). Moved from "Documents To Generate" to "Active Documents" and marked as a completed milestone. Current Target advanced to global_rules_revisionfinal_v10.md.
 
YYYY-MM-DD
 
Initial framework status created.
